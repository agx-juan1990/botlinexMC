import { channels } from './channels.js'; import { roles } from './roles.js'; import { guilds } from './guilds.js'; import { ticketCategories } from './ticketCategories.js'; export const config = { channels, roles, guilds, ticketCategories, dashboardUrl:'https://www.aegox.org', economy:{coinsPerInvite:100, referralDays:10, referralLevel:1} }; export const mentionRole=(id:string)=>`<@&${id}>`;